# Day 11 - NumPy Operations

Today I learned about different operations that can be performed using NumPy arrays in Python.

## What I Learned

- Array addition, subtraction, multiplication and division
- `np.sum()` - finding the total
- `np.mean()` - finding the average
- `np.min()` - finding the minimum value
- `np.max()` - finding the maximum value
- Comparing array elements using conditions
- Boolean filtering in NumPy

## Practice

I practiced NumPy operations using student marks.

```python
marks = np.array([85, 70, 92, 65, 78])